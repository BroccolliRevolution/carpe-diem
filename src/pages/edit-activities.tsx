import { useEffect, useRef, useState } from "react"

function Edit() {
  const [count, setCount] = useState(0)

  return (
    <div>
      <div>edit activities here </div>
    </div>
  )
}
export default Edit
